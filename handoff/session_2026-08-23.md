# Session: 2026-08-23

## Completed
- **Hero section fan** (`components/sections/Hero.tsx`, `Hero.module.css`): extended from 3 to 5 cards, adding `aura-specialists.png` and `blush-specialists.png` as new outer `shotFarLeft`/`shotFarRight` positions, symmetric rotation/z-index geometry. Later swapped the center card (`public/demo-shots/home.png`) for a new, more detailed Loom & Blade screenshot (pure asset replacement, no code change).
- **Booking Site section rebuilt** (`components/sections/BookingSiteSection.tsx`, new `BookingSiteSection.module.css`): replaced the single-`PhoneFrame` two-column placeholder with a Hero-style centered layout and a 6-phone fan (Aura ×2, Blush, Loom & Blade ×2, Telegram bot). Copy updated to mention the bundled Telegram booking bot. Went through several rounds of live-verified tuning:
  - Fixed bottom-edge overflow at laptop viewport heights (measured via `getBoundingClientRect`, not just eyeballed).
  - Reverted an over-aggressive size reduction back to the original natural phone size per user feedback, instead raising fan position and trimming the text-to-fan gap.
  - Fixed the phone "island" (notch) which was fixed-pixel and looked oversized on the compact fan — added `--island-width/height/top` CSS variable overrides on `PhoneFrame.module.css` so standalone usages (Mobile, Notifications) keep their original look while the fan gets a proportionally smaller notch.
  - `PhoneFrame.module.css` also gained a `--phone-width` override hook (backward compatible default).
- **Notifications section rebuilt** (`components/sections/NotificationsSection.tsx`, new `NotificationsSection.module.css`): replaced the single-phone Telegram screenshot with a 3-card fan of real admin-panel screenshots (`admin-sms.png`, `admin-notifications.png`, `admin-templates.png`). Copy rewritten twice: first to replace the old chatbot-only framing with concrete channel/setup info, then simplified again to drop "API key" jargon per user feedback. Screenshots were cropped (via a local Pillow venv) to remove the admin sidebar, since the dense small nav text was the actual cause of the "blurry/muddy" look at small rotated sizes — cropping to just the content panel fixed legibility. Fan cards later enlarged and repositioned lower per follow-up feedback.
- **Favicon / app icon** (`app/favicon.ico`, `app/icon.png`): cropped the ring mark out of `public/ordiset-logo.png` using the exact same crop rectangle Nav.module.css already uses (680×680 at (866,202)), generated a multi-resolution ICO and a 512×512 PNG.
- **Diagnosed and fixed a Next.js dev image-optimizer cache bug**: swapping `public/demo-shots/telegram-bot.png` on disk wasn't reflected in the browser because `next/image`'s optimizer (`.next/dev/cache/images`) serves a cached render keyed by URL, not file content/mtime. Root-caused via `X-Nextjs-Cache: HIT` header inspection; fixed by clearing that cache directory. Worth remembering for any future same-filename asset swap.
- **Contact form wired to a real backend** (`app/api/contact/route.ts` new, `components/ContactForm.tsx`, `.env.local` new/gitignored): after discussing email vs. Telegram bot with the user (recommended Telegram — simpler, no SMTP/domain setup, instant delivery, consistent with the product's own positioning), user supplied a live bot token + chat ID. Verified both directly via curl (`getMe`, `sendMessage`) before wiring anything. New API route validates input server-side and forwards to Telegram; form now does a real `fetch` with a proper `error` status instead of the old fake-delay stub. Verified end-to-end through the actual browser UI (fill → submit → "Thanks" message → field reset), confirmed via network log that `POST /api/contact` returns 200.
- **Small UX fix**: `components/ContactForm.module.css` — submit button was missing `cursor: pointer`, looked "dead" on hover.
- **Social preview (OG/Twitter) + robots.txt** (`app/layout.tsx`, `app/robots.ts` new, `public/og-image.png` new): user provided a new dark/gold logo image; cropped it (2730×1872 → 1200×630, trimmed vertical padding, kept full margins) for the standard social-card ratio. Added `metadataBase`, `description`, full `openGraph`/`twitter` metadata blocks. Added `app/robots.ts` (Next.js metadata-route convention) allowing all crawlers, host `https://ordiset.com`. Verified via curl that both `/robots.txt` and the rendered `<head>`'s `og:*`/`twitter:*` tags resolve correctly to absolute `ordiset.com` URLs.
- **Fixed server deploy failure — root `tsconfig.json` was type-checking `demo-widget`** (`tsconfig.json`): first server deploy attempt failed `next build`'s typecheck with `TS2307: Cannot find module 'lucide-react'` (and ~30 similar) across every `demo-widget/src/**` file. Root cause: `tsconfig.json`'s `include: ["**/*.ts","**/*.tsx",...]` recursively swept the whole repo including `demo-widget` (a fully separate Vite/React app with its own `package.json`/`tsconfig.json`/build, never imported by the Next.js app), while `exclude: ["node_modules"]` only excluded the *root* `node_modules`, not `demo-widget/node_modules` — invisible locally because that folder happened to be installed there, but never installed on the fresh server checkout. Fixed by adding `"demo-widget"` to the root `exclude` array. Verified with a clean `npm run build` locally (passes).
- **Admin panel mobile fixes — two LIGHT-mode rounds**, both planned/coded/reviewed via the `handoff/` protocol (plan → coder → reviewer, all APPROVED):
  - Round 1 (`handoff/admin_mobile_plan.md` / `_feedback.md`): fixed a real bug where the embedded admin demo's mobile hamburger drawer (`demo-widget/src/admin/AdminApp.tsx`) opened empty — its `AdminSidebar` (`demo-widget/src/admin/AdminSidebar.tsx`) carried a hardcoded `hidden lg:flex` class that hid it even inside the drawer that's only shown below `lg`. Added an `alwaysVisible` prop, applied only to the mobile-drawer instance. Also (per user's explicit choice between two options) made `AdminPanelSection.tsx` switch to a phone-frame (`PhoneFrame` + `DemoStage` `fixedViewport`) treatment on narrow/short viewports, matching `MobileSection.tsx`'s pattern, toggled via the site's existing `899px`/`700px` "static-layout" breakpoint (`STATIC_STACK_QUERY` in `ScrollDock.tsx`).
  - Round 2 (`handoff/admin_mobile_fixes2_plan.md` / `_feedback.md`): after seeing round 1 live, user asked to revert the phone-frame variant — the admin section's framing looked inconsistent with `PreviewSection.tsx`, and a dedicated real-phone preview already exists lower on the page (`MobileSection`), making a second one redundant. Reverted `AdminPanelSection.tsx` to a single unconditional `WindowChrome`+`DemoStage` embed (matching `PreviewSection.tsx` exactly) and removed the now-unused `.mobileOnly`/`.desktopOnly` CSS. Also fixed a genuinely new bug found in the same screenshots: the admin sidebar's collapsed (icon-only, `w-[72px]`) rail visually clipped, because its header row rendered two 32px icons (scissors logo + collapse toggle) that need ~104px in a 72px space — there was already a runtime-only CSS patch for this in `DemoStage.tsx` (`__sidebar-clip-fix`) that just barely made it fit. Per user's request, fixed at the root instead: the scissors logo `<div>` in `AdminSidebar.tsx` is now conditional on `open`, so the collapsed rail shows only the burger toggle — and removed the now-obsolete runtime patch from `DemoStage.tsx` entirely.
  - Rebuilt `demo-widget` (`npm run build`) and synced its output into `public/demo-app/` via `rsync -a --delete` (a committed static copy, not auto-synced) so both this round's fixes and round 1's `alwaysVisible` fix are actually present in what gets served — reviewer verified `demo-widget/dist/` and `public/demo-app/` are now byte-identical and the deployed bundle contains the fix.
  - `npx tsc --noEmit` (repo root) and the `demo-widget` build both verified clean after every round.
- **`DemoStage` size-jump bug — three-round chase, resolved by reusing an existing pattern instead of tuning the broken one:**
  - Round A (`handoff/demostage_resize_plan.md` / `_feedback.md`): user reported (with real-iPhone screenshots) that `PreviewSection`'s embedded demo visibly changes scale after navigating within it and going back (e.g. home → click a specialist → booking view → back → home now renders smaller, text wraps differently). Root-caused in `components/DemoStage.tsx`: `syncNeed()` computed required iframe height as a **monotonic maximum** — could grow but never shrink back down once a taller in-app view had been visited — confirmed by the component's own pre-existing doc comment on the `fixedViewport` prop, which already described this exact defect. Fixed `syncNeed` to always track the *current* content height instead of a running max; trimmed the now-stale doc comment. Reviewer caught one leftover stale comment elsewhere in the file (in the `autoClickText` reset flow) — fixed directly by the orchestrator, no re-review round needed.
  - This fix was real but **insufficient**: user re-tested on an actual phone (not Chrome DevTools mobile emulation — which does not simulate Safari/Chrome's mobile address-bar show/hide animation during scroll) and the jump was still there. Confirms a suspicion raised earlier in this session (during the original mobile-jump investigation) that headless/CDP emulation cannot reproduce this class of bug at all.
  - Round B (`handoff/preview_fixedviewport_plan.md` / `_feedback.md`): gave `PreviewSection` a mobile-only variant using `fixedViewport={{width:390,height:844}}` (same mechanism `MobileSection.tsx` already uses successfully) — but kept the plain `WindowChrome` frame (no phone bezel), to avoid regressing the "consistent framing" preference from the admin-panel rounds. Reviewed and approved (verified a subtle CSS cascade question correctly: `.mobileOnly` needed to stay a pure display-toggle with no centering rules of its own, since `WindowChrome` needs `.growFull`'s `align-items:stretch` to fill its box height). **User pushed this to production and reported it rendered "completely abnormal" on a real device** — rejected outright, no further tuning requested.
  - Round C (`handoff/reuse_phoneframe_plan.md` / `_feedback.md`, final/shipped): per explicit user instruction, discarded the `WindowChrome`+`fixedViewport` hybrid entirely and instead reused the **exact already-proven `PhoneFrame` + `fixedViewport` pattern** from `MobileSection.tsx` (which was never touched or reported broken throughout this whole chase) as the mobile variant for **both** `PreviewSection.tsx` and `AdminPanelSection.tsx` (restoring — and this time keeping — the phone-frame variant for Admin that round 2 of the admin-panel work had reverted). Desktop is unchanged for both (`WindowChrome` auto-fit). `sections.module.css`'s `.mobileOnly` needed its flex-centering rules restored (a `PhoneFrame` needs them; the `WindowChrome` variant from round B didn't). All 5 `DemoStage` instances on the page now have distinct `title` attributes (grep-verified). Reviewed and approved; `npm run build` and `tsc --noEmit` both clean.

## In Progress
- Nothing left mid-implementation. Every task this session was completed and reviewer-approved (or user-browser-verified) before being reported. The final `DemoStage` mobile fix (round C above) has not yet been confirmed working on the user's real device — they were about to push and test when this session summary was written.

## Next Steps
1. **User pushes round C (`reuse_phoneframe`) to production and re-verifies on a real phone**: both `#preview` and `#admin` should now render inside a phone bezel on narrow screens with no size jump while scrolling or navigating within the embedded demo, and desktop should look unchanged. This is the immediate next action — everything else in this list is secondary to confirming this actually holds on a real device this time, unlike round B.
2. Once confirmed: also re-verify the admin-panel mobile fixes from earlier this session on the live device — (a) the `tsconfig.json` fix lets `next build` pass on the server, (b) the admin demo's mobile hamburger now shows nav items, (c) the collapsed desktop sidebar no longer clips the scissors/menu icons. `demo-widget` was already rebuilt and `public/demo-app/` synced earlier this session for these — no further rebuild needed unless something regresses.
3. `app/sitemap.ts` — offered earlier, still not built (declined implicitly, not requested again this session).

## Decisions Made
- **`demo-widget` must never be part of the root Next.js typecheck** — it's a fully separate Vite app with its own build pipeline and is never imported by the Next.js code; excluding it in `tsconfig.json` is the correct fix, not installing its `node_modules` on the server.
- **User's own screenshots/live testing overrode an earlier explicit choice**: user initially picked the phone-frame treatment for `AdminPanelSection` mobile (over an AskUserQuestion), but reversed that after seeing it live next to `PreviewSection`'s plain `WindowChrome` look — inconsistency between the two, plus redundancy with the existing `MobileSection` phone preview, outweighed the theoretical benefit of "true" mobile-width framing. Lesson: a user's stated preference for a not-yet-seen UI direction is provisional; confirm again once it's actually visible.
- **Root-cause fixes preferred over papering-over patches**: the collapsed-sidebar clipping already had a working runtime CSS patch (`__sidebar-clip-fix` in `DemoStage.tsx`), but per user's request the actual fix (hide the logo icon when collapsed) was applied at the source (`AdminSidebar.tsx`) and the now-unnecessary patch was deleted rather than left as dead code.
- **All three "fan" sections (Hero, Booking Site, Notifications) now share one visual pattern**: full-width centered text block (`containerWide` + `stack`), with a rotated card/phone fan below it, each with its own dedicated `<Section>.module.css`. Booking Site and Notifications were both restructured from their original two-column split layouts to match Hero's established look, per explicit user approval each time.
- **CSS custom properties over prop-drilling or new component variants** for per-instance sizing overrides on shared components (`PhoneFrame`'s `--phone-width`, `--island-width/height/top`): keeps the default/standalone look byte-for-byte identical while letting a specific fan context override just what it needs.
- **Inline `style` used for one-off spacing/font-size overrides** (e.g. Booking Site's and Notifications' `.grow` margin-top) instead of fighting CSS-module cascade/specificity against the shared `sections.module.css` rules — simpler and unambiguous for single-instance tweaks.
- **Cropping admin screenshots to remove the sidebar** rather than shrinking further or leaving as-is: the dense small sidebar nav text was the actual legibility problem at small rotated sizes, not overall resolution — this was verified by direct pixel inspection before cropping.
- **Telegram bot over email for the contact form**, per explicit user choice after a short recommendation (simplicity, no SMTP setup, consistent with the product's own "everything through the bot" positioning). Token/chat ID stored in `.env.local` (already covered by `.gitignore`), never hardcoded into committed source.
- **ordiset.com confirmed as the production domain** this session (via direct question, since nothing in the repo referenced any domain) — now baked into `metadataBase`, OpenGraph URLs, and `robots.ts`.
- **Reuse a proven pattern over continuing to tune a broken one**: after two rounds of trying to fix/adapt the auto-fit `DemoStage` path for mobile (monotonic-max bug fix, then a `WindowChrome`+`fixedViewport` hybrid), both of which failed to fully resolve the real-device jump, the user's own instinct — stop inventing new variants, just reuse `MobileSection`'s already-working `PhoneFrame`+`fixedViewport` instance elsewhere — was the actual fix. Lesson: when a real device contradicts DevTools emulation repeatedly, prefer copying a component/config that's already proven on a real device over continuing to adjust the broken one, even if it means a less "custom" visual result.
- **Chrome DevTools mobile emulation cannot be trusted for this codebase's mobile-jump class of bugs** — it does not simulate the mobile browser address-bar show/hide animation during scroll, which is the suspected real trigger. Confirmed twice this session by user real-device testing contradicting DevTools/headless-CDP results. Any future "elements resize/jump on mobile" report should be tested on/assumed to require a real device, not just emulation.

## Blockers
- None outright. All admin-panel and `DemoStage`/mobile-framing fixes from this session are uncommitted local changes — the live site (`ordiset.com`) reflects only whatever the user has manually pushed mid-session (they pushed the now-rejected round-B `WindowChrome`+`fixedViewport` Preview variant to prod at one point; unclear from this session alone whether that's since been overwritten by round C locally without a fresh push — confirm current prod state before assuming it matches the local working copy). Commit/deploy of the final round-C state is the immediate next step.
